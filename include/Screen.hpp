#ifndef _SCREEN_INC
#define _SCREEN_INC

// Used to hold current screen of menu
enum Screen {
	READY,
	SPEED,
	DELAY,
	LENGTH,
	CHANGE_SPEED,
	CHANGE_DELAY,
	CHANGE_LENGTH,
	OFFSET,
	MANUAL_MOVEMENT,
};

#endif
